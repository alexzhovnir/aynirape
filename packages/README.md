# packages/

Shared, versioned-together code used by the apps (empty until needed). Likely first
additions:

- `types/` — shared TypeScript types / Store API contracts
- `config/` — shared tsconfig, eslint, prettier
- `ui/` — shared storefront components, if any are reused across surfaces
